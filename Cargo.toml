[package]
name = "game-op"
version = "0.1.0"
edition = "2021"
description = "Cross-platform game performance session optimizer"
license = "MIT"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }
